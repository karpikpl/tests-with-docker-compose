describe('GET contacts', () => {
  test('if this project was better organized - a unit test would be here ;)', async () => {
    // arrange

    // act

    // assert
    expect(true).toBe(true);
  });
});
